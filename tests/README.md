# Tests

Tests will verify the project at multiple levels.

## Planned test layers

- schema tests for device profiles
- build reproducibility checks
- kernel boot checks
- systemd target checks
- Wayland session checks
- shell startup checks
- QEMU integration tests
- later: physical-device smoke tests

The v0.1 definition of done is documented in `docs/v0.1.md`.
