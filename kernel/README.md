# Kernel

Kernel configuration, build metadata and future patches belong here.

For v0.1 the target is AArch64 under QEMU. We should prefer an upstream Linux kernel and a minimal configuration before introducing device-specific patches.

Do not add a physical-phone kernel here yet. Physical devices should eventually have device-specific build metadata under `devices/`.
